<template name="opportunities">
  <h1>Opportunities</h1>
  <div class="row">
    <div class="opportunities">
      <div class="col-lg-10">
        <table class="table">
          <thead>
            <tr>
            <th>Opportunity Name</th>
            <th>Owner</th>
            </tr>
	  </thead>
	  <tbody>
	    {{#each opportunities}}
	      {{> opportunityListRow}}
	    {{/each}}
	  </tbody>
	</table>
      </div>
    </div>
  </div>
</template>

<template name="opportunityListRow">
  <tr>
    <td><a href="{{pathFor 'opportunityView'}}"> {{this.name}} </a></td>
    <td>{{this.owner_id}}</td>
  </tr>
</template>

<template name="opportunityView">
  <h1><a href="{{pathFor 'opportunities'}}">Opportunity</a></h1>
  <h3>{{name}}</h3>
</template>
